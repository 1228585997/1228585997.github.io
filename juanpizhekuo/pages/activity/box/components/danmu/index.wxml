<view class="root">
    <view class="linear" style="animation: first {{item.time}}s linear forwards; top:{{item.top}}%; " wx:if="{{item.display}}" wx:for="{{danmuData}}" wx:key="id">
        <text class="text">{{item.text}}</text>
    </view>
</view>
